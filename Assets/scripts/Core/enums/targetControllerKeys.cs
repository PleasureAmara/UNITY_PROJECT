namespace localizer.core.enums
{
    /// <summary>
    /// The enum for controlling stages of movement of the player at game start. 
    /// </summary>
    public enum TargetControllerKeys
    {
        Select,
        Move,
        SnapTurn,
        QuitGame,
        Empty
    }
}